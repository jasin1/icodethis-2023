

const checkBox = document.querySelectorAll('.eval-check-inside');

const css_of_after = window.getComputedStyle(checkBox, ':before');

console.log(css_of_after.position);


//-------------- calculate dates intervals -------------- //

const startDate = 0;

const endDate = 10;